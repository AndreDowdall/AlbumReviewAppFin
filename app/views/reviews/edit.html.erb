<h2>Edit Review</h2>

<%= simple_form_for([@album, @review]) do |f| %>
	<div id="rating-form">
		<label>Rating</label>
	</div>
	<%= f.input :rating %>
	<%= f.input :comment %>
	<%= f.button :submit %>
<% end %>

